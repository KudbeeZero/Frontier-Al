FRONTIER — UI Wiring Pass (Prompt 3 of N)
Project: FRONTIER MMO Strategy Game

CONTEXT: The server now accepts and processes crystalBurned in attacks and 
writes influenceDamage to resolved battles. The UI still sends 0 crystal and 
doesn't show influence. This prompt wires both into the UI.

Three files to edit. Surgical changes only.

DO NOT:
- Remove the troops slider from AttackModal — leave it exactly as it is
- Change any server files
- Touch any component other than AttackModal.tsx, GameLayout.tsx, and LandSheet.tsx
- Change routing, hooks, or API call mechanics
- Rename any props or callback signatures on other components

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EDIT 1 — client/src/components/game/AttackModal.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step A — Update the onAttack prop signature to include crystal.

FIND:
  onAttack: (troops: number, iron: number, fuel: number, commanderId?: string) => void;

REPLACE WITH:
  onAttack: (troops: number, iron: number, fuel: number, crystal: number, commanderId?: string) => void;

Step B — Add a crystal state variable alongside the existing ones.

FIND:
  const [troops, setTroops] = useState(1);
  const [extraIron, setExtraIron] = useState(0);
  const [extraFuel, setExtraFuel] = useState(0);

REPLACE WITH:
  const [troops, setTroops] = useState(1);
  const [extraIron, setExtraIron] = useState(0);
  const [extraFuel, setExtraFuel] = useState(0);
  const [extraCrystal, setExtraCrystal] = useState(0);

Step C — Update the attackerPower calculation to include crystal.

FIND:
  const attackerPower = troops * 10 + extraIron * 0.5 + extraFuel * 0.8 + commanderBonus;

REPLACE WITH:
  const attackerPower = troops * 10 + extraIron * 0.5 + extraFuel * 0.8 + extraCrystal * 1.2 + commanderBonus;

Step D — Update handleSubmit to pass crystal.

FIND:
  const handleSubmit = () => {
    onAttack(troops, totalCost.iron, totalCost.fuel, selectedCommanderId ?? undefined);
  };

REPLACE WITH:
  const handleSubmit = () => {
    onAttack(troops, totalCost.iron, totalCost.fuel, extraCrystal, selectedCommanderId ?? undefined);
  };

Step E — Update the canAfford check to include crystal.

FIND:
  const canAfford = attacker.iron >= totalCost.iron && attacker.fuel >= totalCost.fuel;

REPLACE WITH:
  const canAfford = attacker.iron >= totalCost.iron && attacker.fuel >= totalCost.fuel && attacker.crystal >= extraCrystal;

Step F — Add crystal slider to the UI. Add it directly after the Extra Fuel 
slider block and before the Power Display block.

The Extra Fuel slider block ends with its closing </div>. After that closing 
</div>, ADD this new block:

            <div>
              <div className="flex items-center justify-between mb-2">
                <span className="text-sm font-display uppercase tracking-wide flex items-center gap-2">
                  <span className="w-3 h-3 rounded-full bg-cyan-400 inline-block" />
                  Crystal Boost
                  <span className="text-[10px] text-muted-foreground normal-case font-normal">(×1.2 power)</span>
                </span>
                <span className="font-mono text-sm text-cyan-400">{extraCrystal}</span>
              </div>
              <Slider
                value={[extraCrystal]}
                onValueChange={([v]) => setExtraCrystal(v)}
                min={0}
                max={Math.max(0, attacker.crystal)}
                step={1}
                className="w-full"
                data-testid="slider-extra-crystal"
              />
              {attacker.crystal === 0 && (
                <p className="text-[10px] text-muted-foreground mt-1">Mine volcanic parcels to acquire crystal.</p>
              )}
            </div>

Step G — Update the Total Cost display to include crystal.

FIND the Total Cost section. It currently shows iron and fuel spans.
After the fuel span, ADD:

              {extraCrystal > 0 && (
                <span className="font-mono flex items-center gap-1 text-cyan-400">
                  <span className="w-2.5 h-2.5 rounded-full bg-cyan-400 inline-block" /> {extraCrystal}
                </span>
              )}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EDIT 2 — client/src/components/game/GameLayout.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step A — Update handleAttackConfirm to receive and pass crystal.

FIND:
  const handleAttackConfirm = async (troops: number, iron: number, fuel: number, commanderId?: string) => {

REPLACE WITH:
  const handleAttackConfirm = async (troops: number, iron: number, fuel: number, crystal: number, commanderId?: string) => {

Step B — Pass crystal into the mutation payload.

FIND:
      { attackerId: player.id, targetParcelId: selectedParcelId, troopsCommitted: troops, resourcesBurned: { iron, fuel }, commanderId },

REPLACE WITH:
      { attackerId: player.id, targetParcelId: selectedParcelId, troopsCommitted: troops, resourcesBurned: { iron, fuel }, crystalBurned: crystal, commanderId },

Step C — Pass crystal into queueAttackAction (for the optimistic queue).

FIND:
    queueAttackAction(selectedParcel.plotId, troops, iron, fuel);

REPLACE WITH:
    queueAttackAction(selectedParcel.plotId, troops, iron, fuel, crystal);

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EDIT 3 — client/src/hooks/useBlockchainActions.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step A — Update queueAttackAction signature to accept crystal.

FIND:
  const queueAttackAction = useCallback(
    (plotId: number, troops: number, iron: number, fuel: number) => {
      if (isReady && address) {
        console.log(`[ACTION-DEBUG] queueAttackAction | path: enqueueGameAction→batch | plotId: ${plotId} | troops: ${troops} | ts: ${Date.now()}`);
        enqueueGameAction("attack", plotId, { troops, iron, fuel });
      }
    },
    [isReady, address]
  );

REPLACE WITH:
  const queueAttackAction = useCallback(
    (plotId: number, troops: number, iron: number, fuel: number, crystal: number = 0) => {
      if (isReady && address) {
        console.log(`[ACTION-DEBUG] queueAttackAction | path: enqueueGameAction→batch | plotId: ${plotId} | troops: ${troops} | crystal: ${crystal} | ts: ${Date.now()}`);
        enqueueGameAction("attack", plotId, { troops, iron, fuel, crystal });
      }
    },
    [isReady, address]
  );

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EDIT 4 — client/src/components/game/LandSheet.tsx
Add influence display to the parcel stats row
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FIND the stats row that shows defenseLevel. It will be inside a grid and 
contain a Shield icon and the text "Defense". The block looks approximately 
like this:

              <Shield className={cn("w-4 h-4 mx-auto mb-1", ...)} />
              ...
              <span ...>{parcel.defenseLevel}</span>
              ...
              <span ...>Defense</span>

That stat block is one cell in a grid. After that entire defense cell, 
ADD a new stat cell for influence:

            <div className="text-center">
              <div className={cn(
                "w-4 h-4 mx-auto mb-1 rounded-full border-2",
                (parcel.influence ?? 100) > 66
                  ? "border-green-500 bg-green-500/20"
                  : (parcel.influence ?? 100) > 33
                  ? "border-yellow-500 bg-yellow-500/20"
                  : "border-red-500 bg-red-500/20"
              )} />
              <span className={cn(
                "font-mono text-sm font-bold",
                (parcel.influence ?? 100) > 66 ? "text-green-400"
                  : (parcel.influence ?? 100) > 33 ? "text-yellow-400"
                  : "text-red-400"
              )}>
                {parcel.influence ?? 100}
              </span>
              <p className="text-[10px] text-muted-foreground uppercase">Influence</p>
            </div>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. npx tsc --noEmit → zero errors
2. Open AttackModal — the Crystal Boost slider appears, shows 0 when 
   player has no crystal, max = player.crystal
3. Confirm win chance percentage updates in real-time when crystal slider moves
4. Confirm the troops slider is still present and unchanged
5. Confirm the Total Cost section shows crystal only when extraCrystal > 0
6. In LandSheet, confirm influence shows as a green/yellow/red indicator
7. Fire a real attack using crystal — confirm crystalBurned > 0 in the 
   battle record in the DB after submission
