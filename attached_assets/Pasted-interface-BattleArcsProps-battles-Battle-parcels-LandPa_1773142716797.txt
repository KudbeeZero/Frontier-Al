interface BattleArcsProps {
  battles: Battle[];
  parcels: (LandParcel | SlimParcel)[];
  players: Player[];
  currentPlayerId: string | null;
}

// Arc lift factor — how high above the globe the bezier control point rises.
// Scaled by great-circle distance so short arcs stay low, long arcs arc high.
const ARC_LIFT_BASE   = 1.4;
const ARC_LIFT_SCALE  = 0.6;
const PROJECTILE_SIZE = 0.018;
const ARC_TUBE_RADIUS = 0.006;
const ARC_SEGMENTS    = 48;
const FADE_DURATION   = 1500; // ms

function buildArcCurve(from: THREE.Vector3, to: THREE.Vector3): THREE.QuadraticBezierCurve3 {
  const mid = from.clone().add(to).multiplyScalar(0.5);
  const dist = from.distanceTo(to);
  const lift = ARC_LIFT_BASE + dist * ARC_LIFT_SCALE;
  mid.normalize().multiplyScalar(GLOBE_RADIUS * lift);
  return new THREE.QuadraticBezierCurve3(from, mid, to);
}

function SingleArc({
  arc,
  now,
}: {
  arc: ArcData;
  now: number;
}) {
  const tubeRef       = useRef<THREE.Mesh>(null!);
  const projectileRef = useRef<THREE.Mesh>(null!);
  const progressRef   = useRef(0);
  const opacityRef    = useRef(1);

  const curve = useMemo(
    () => buildArcCurve(arc.fromVec, arc.toVec),
    [arc.fromVec, arc.toVec]
  );

  const tubeGeo = useMemo(
    () => new THREE.TubeGeometry(curve, ARC_SEGMENTS, ARC_TUBE_RADIUS, 6, false),
    [curve]
  );

  // Color: cyan for player attacking, red for enemy attacking player, orange for AI vs AI
  const arcColor = arc.isPlayerAttacker ? "#00e5ff" : "#ff1744";

  useFrame((_, delta) => {
    // Fade out if resolved
    if (arc.resolvedAt !== null) {
      const elapsed = Date.now() - arc.resolvedAt;
      opacityRef.current = Math.max(0, 1 - elapsed / FADE_DURATION);
    } else {
      opacityRef.current = 1;
    }

    // Advance projectile along curve
    progressRef.current = (progressRef.current + delta * 0.35) % 1;
    const pos = curve.getPoint(progressRef.current);

    if (projectileRef.current) {
      projectileRef.current.position.copy(pos);
      (projectileRef.current.material as THREE.MeshBasicMaterial).opacity = opacityRef.current;
    }
    if (tubeRef.current) {
      (tubeRef.current.material as THREE.MeshBasicMaterial).opacity =
        opacityRef.current * 0.55;
    }
  });

  if (opacityRef.current <= 0) return null;

  return (
    <group>
      {/* Arc tube */}
      <mesh ref={tubeRef} geometry={tubeGeo}>
        <meshBasicMaterial
          color={arcColor}
          transparent
          opacity={0.55}
          depthWrite={false}
        />
      </mesh>

      {/* Moving projectile dot */}
      <mesh ref={projectileRef} position={arc.fromVec}>
        <sphereGeometry args={[PROJECTILE_SIZE, 8, 8]} />
        <meshBasicMaterial
          color={arcColor}
          transparent
          opacity={1}
          depthWrite={false}
        />
      </mesh>

      {/* Glow halo at destination */}
      <mesh position={arc.toVec}>
        <sphereGeometry args={[PROJECTILE_SIZE * 2.5, 8, 8]} />
        <meshBasicMaterial
          color={arc.isPlayerAttacker ? "#00e5ff" : "#ff1744"}
          transparent
          opacity={0.25}
          depthWrite={false}
        />
      </mesh>
    </group>
  );
}

function BattleArcs({ battles, parcels, players, currentPlayerId }: BattleArcsProps) {
  // Build a fast lookup: parcel.id → { lat, lng }
  const parcelLatLng = useMemo(() => {
    const m = new Map<string, { lat: number; lng: number }>();
    parcels.forEach(p => m.set(p.id, { lat: p.lat, lng: p.lng }));
    return m;
  }, [parcels]);

  // Build a fast lookup: player.id → first owned parcel id
  const playerFirstParcel = useMemo(() => {
    const m = new Map<string, string>();
    players.forEach(p => {
      if (p.ownedParcels && p.ownedParcels.length > 0) {
        m.set(p.id, p.ownedParcels[0]);
      }
    });
    return m;
  }, [players]);

  // Derive arc data from battles — only pending + recently resolved
  const arcs = useMemo<ArcData[]>(() => {
    const now = Date.now();
    const result: ArcData[] = [];

    for (const battle of battles) {
      // Skip resolved battles that finished more than FADE_DURATION ago
      if (
        battle.status === "resolved" &&
        battle.resolveTs &&
        now - battle.resolveTs > FADE_DURATION
      ) continue;

      // Defender location — from targetParcelId
      const defCoord = parcelLatLng.get(battle.targetParcelId);
      if (!defCoord) continue;

      // Attacker location — first owned parcel of attacker
      const attackerParcelId = playerFirstParcel.get(battle.attackerId);
      if (!attackerParcelId) continue;
      const atkCoord = parcelLatLng.get(attackerParcelId);
      if (!atkCoord) continue;

      // Skip arcs where attacker and defender are the same location
      if (
        Math.abs(atkCoord.lat - defCoord.lat) < 0.01 &&
        Math.abs(atkCoord.lng - defCoord.lng) < 0.01
      ) continue;

      const fromVec = latLngToVec3(atkCoord.lat, atkCoord.lng, GLOBE_RADIUS * 1.01);
      const toVec   = latLngToVec3(defCoord.lat, defCoord.lng, GLOBE_RADIUS * 1.01);

      result.push({
        battleId:          battle.id,
        fromVec,
        toVec,
        isPlayerAttacker:  battle.attackerId === currentPlayerId,
        resolvedAt:        battle.status === "resolved" ? battle.resolveTs : null,
      });
    }

    return result;
  }, [battles, parcelLatLng, playerFirstParcel, currentPlayerId]);

  if (arcs.length === 0) return null;

  const now = Date.now();

  return (
    <>
      {arcs.map(arc => (
        <SingleArc key={arc.battleId} arc={arc} now={now} />
      ))}
    </>
  );
}

---

EDIT 2 — PlanetGlobe.tsx: Add Battle import to shared/schema

Find the import line:
import type { LandParcel, Player } from "@shared/schema";

Replace with:
import type { LandParcel, Player, Battle } from "@shared/schema";

---

EDIT 3 — PlanetGlobe.tsx: Add SlimParcel import

Find the import line that was updated to include Battle (from Edit 2):
import type { LandParcel, Player, Battle } from "@shared/schema";

Replace with:
import type { LandParcel, Player, Battle, SlimParcel } from "@shared/schema";

NOTE: If SlimParcel does not yet exist in shared/schema.ts (slim broadcast
pass not yet applied), keep this import as:
import type { LandParcel, Player, Battle } from "@shared/schema";
and use LandParcel[] instead of (LandParcel | SlimParcel)[] throughout
BattleArcsProps and BattleArcs. The component logic is identical either way.

---

EDIT 4 — PlanetGlobe.tsx: Add battles to SceneProps and Scene

Find SceneProps interface. Add battles prop:
Find:
  targetLat: number | null;
  targetLng: number | null;

Add immediately after those two lines:
  battles: Battle[];

Find the Scene function signature destructure. Add battles:
Find:
function Scene({ parcels, players, currentPlayerId, selectedPlotId, onPlotSelect, controlsRef, targetLat, targetLng, replayEvents, replayTime, replayVisibleTypes }: SceneProps) {

Replace with:
function Scene({ parcels, players, currentPlayerId, selectedPlotId, onPlotSelect, controlsRef, targetLat, targetLng, battles, replayEvents, replayTime, replayVisibleTypes }: SceneProps) {

NOTE: If the Globe-Camera pass has not been applied yet and targetLat/targetLng
do not exist in SceneProps, add battles alongside the existing SceneProps fields
without adding targetLat/targetLng.

---

EDIT 5 — PlanetGlobe.tsx: Render BattleArcs inside Scene

Inside the Scene return, add BattleArcs after AtmosphereGlow and before
the closing fragment tag </>.

Find:
      <AtmosphereGlow />
      <OrbitControls

Insert between them:
      <BattleArcs
        battles={battles}
        parcels={parcels}
        players={players}
        currentPlayerId={currentPlayerId}
      />

---

EDIT 6 — PlanetGlobe.tsx: Add battles to PlanetGlobeProps

Find PlanetGlobeProps interface:
Find:
  replayEvents?: WorldEvent[];

Add immediately before it:
  battles?: Battle[];

Find the PlanetGlobe function destructure:
Find:
  replayEvents,

Add immediately before it:
  battles = [],

---

EDIT 7 — PlanetGlobe.tsx: Pass battles into Scene

Find the Scene JSX inside the Canvas:
Find:
          targetLat={selectedParcel?.lat ?? null}
          targetLng={selectedParcel?.lng ?? null}

Add immediately after those two lines:
          battles={battles}

NOTE: If Globe-Camera pass has not been applied, find the Scene JSX and
add battles={battles} alongside the existing props.

---

EDIT 8 — GameLayout.tsx: Pass battles to PlanetGlobe

Find the PlanetGlobe usage in GameLayout (the 3D map branch):
Find:
            <PlanetGlobe
              parcels={gameState.parcels}
              players={gameState.players}
              currentPlayerId={player?.id || null}
              selectedParcelId={selectedParcelId}
              onParcelSelect={setSelectedParcelId}
              onAttack={handleAttackClick}
              onMine={handleMine}
              onBuild={() => { /* LandSheet handles upgrades — stay on map */ }}
              className="absolute inset-0 w-full h-full"
              replayEvents={replayEvents}
              replayTime={replayTime}
              replayVisibleTypes={replayVisibleTypes}
            />

Replace with:
            <PlanetGlobe
              parcels={gameState.parcels}
              players={gameState.players}
              currentPlayerId={player?.id || null}
              selectedParcelId={selectedParcelId}
              onParcelSelect={setSelectedParcelId}
              onAttack={handleAttackClick}
              onMine={handleMine}
              onBuild={() => { /* LandSheet handles upgrades — stay on map */ }}
              className="absolute inset-0 w-full h-full"
              battles={gameState.battles}
              replayEvents={replayEvents}
              replayTime={replayTime}
              replayVisibleTypes={replayVisibleTypes}
            />

---

Verification checklist:
- tsc passes with zero errors
- BattleArcs component exists in PlanetGlobe.tsx
- SingleArc component exists in PlanetGlobe.tsx
- Battle is imported from @shared/schema
- PlanetGlobeProps has battles?: Battle[]
- SceneProps has battles: Battle[]
- GameLayout passes battles={gameState.battles} to PlanetGlobe
- When a battle is pending, a glowing arc renders from attacker
  to defender on the globe surface
- A dot animates along the arc continuously
- A dim halo glows at the defender's parcel
- Player-initiated attacks render cyan arcs (#00e5ff)
- Enemy attacks against the player render red arcs (#ff1744)
- Resolved battles fade out over 1.5 seconds then disappear
- No arcs render when battles array is empty
- No changes to PlotOverlay, GlobeTerrain, AtmosphereGlow,
  StarField, ParcelHUD, FactionLegend, GlobeEventOverlays,
  CameraController, or any server files
