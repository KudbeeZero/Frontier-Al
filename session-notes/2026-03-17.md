# Session Notes — 2026-03-17

## Task: Add "Find Plot" Button to SubParcelGrid

### Context
Previous session identified that the subdivision UI (SubParcelGrid in LandSheet.tsx) lacked a dedicated navigation button to fly the globe camera to the plot. Navigation existed only in InventoryPanel and CommandCenterPanel via `onSelectParcel`.

### Changes Made

**`client/src/components/game/LandSheet.tsx`**
- Added `onNavigate?: () => void` to `SubParcelGridProps`
- Added `onNavigateToPlot?: () => void` to `LandSheetProps`
- Threaded `onNavigateToPlot` → `onNavigate` prop on `SubParcelGrid`
- Added "Find Plot" button (MapPin icon) in the SubParcelGrid header row, visible only when `onNavigate` is provided

**`client/src/components/game/GameLayout.tsx`**
- Passed `onNavigateToPlot={() => flyToParcelOnMap(selectedParcel.id)}` to `<LandSheet>`

### Result
- Subdivided plot cards now show a small "Find Plot" button (📍 Find Plot) in the Sub-Parcels section header
- Clicking it triggers `flyToParcelOnMap`, switching to map tab and flying the camera to the plot
- Button only renders when the callback is wired (optional prop — safe default)

### Branch
`claude/add-plot-button-subdivision-TOUl7`

### Commit
`f97b299` — feat: add Find Plot button to SubParcelGrid in LandSheet
