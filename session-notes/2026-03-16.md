# Session Notes — 2026-03-16

## Branch
`claude/remove-checkly-organize-notes-ZYZg3`

---

## Tasks Completed

### 1. Checkly Removal
- Deleted entire `__checks__/` directory (contained Checkly monitors and browser checks)
  - `api.check.ts` — API check using `@checkly/constructs`
  - `url.check.ts` — URL monitor
  - `heartbeat.check.ts` — Heartbeat monitor
  - `homepage.spec.ts` — Checkly browser check (Playwright)
  - `custom.spec.ts` — Checkly browser check using `PUBLIC_BASE_URL`
- Removed `"checkly": "^5.4.0"` from `package.json` devDependencies
- No `checkly.config.ts` file existed at root

### 2. Session Notes Directory
- Created `session-notes/` directory at repo root
- Will be used to track all future sessions

---

## Current Status

### Public Base URL
- **No public base URL currently set.**
- The old URL `https://ascendancyalgo.xyz/` (found in `custom.spec.ts`) is no longer active / subject to change.
- `PUBLIC_BASE_URL` env var referenced in code — to be updated when a new URL is established.
- **Action needed:** Update `PUBLIC_BASE_URL` once new base URL is confirmed.

---

## Notes / Context

- This is a large-scale strategy game with:
  - 3D planetary map
  - Parcel system
  - AI factions
  - Blockchain interactions
- Tech stack: Node/TypeScript, React, Vite, PostgreSQL (Drizzle ORM), WebSockets, Passport auth, WalletConnect

---

## Next Steps (carry to next session)

- [x] Confirm and set new `PUBLIC_BASE_URL` when available → documented in replit.md as required mainnet action
- [ ] Review any remaining hardcoded URLs in the codebase
- [ ] Continue game development

---

## Session 2 — Replit Import Prep + Mainnet Readiness

### Branch
`claude/prepare-replit-mainnet-CdfB6`

### Tasks Completed

#### 1. `.replit` — Dev Environment Fix
- Added `NODE_ENV = "development"` to `[env]` block
- Previously only `PORT = "5000"` was set; this makes dev mode explicit in Replit shell

#### 2. `ENV_VARS.md` — Replit Secrets Panel + Mainnet Toggle
- Added **Replit Secrets Panel** section: table of every secret keyed for Replit's Secrets UI, split by required / mainnet-only / optional
- Added **Mainnet Toggle Checklist**: exact env var values to paste when switching testnet → mainnet
- Documented `FORCE_NEW_ASA=true` first-run pattern for mainnet ASA creation

#### 3. `replit.md` — Import Guide + Pre-Launch Checklist
- Added **Replit Import Instructions**: step-by-step (import repo → add secrets → click Run → verify health → verify frontend)
- Added **Required Secrets for Replit**: consolidated table with required / mainnet-only / optional columns
- Added **Mainnet Preparation**: what's already done in code vs. what requires human action (wallet creation, funding, ASA ID recording)
- Added **Pre-Launch Checklist**: ordered, checkbox-formatted — covers admin wallet, secrets, first-boot ASA creation, wallet test, WebSocket test, launch announcement

### What Is Already Mainnet-Ready (No Code Changes Needed)

- `server/services/chain/client.ts` — fully network-agnostic; testnet/mainnet via `ALGORAND_NETWORK` env var
- All Algorand node URLs are env-driven (`ALGOD_URL`, `INDEXER_URL`, `VITE_ALGOD_URL`, `VITE_INDEXER_URL`)
- `assertChainConfig()` enforces required secrets at startup with clear error messages
- `FORCE_NEW_ASA` flag exists for safe first-run ASA creation on any network
- NFT metadata URLs use `PUBLIC_BASE_URL` (no hardcoded domain)
- `REPLIT_DOMAINS` fallback so NFT minting works in Replit dev without `PUBLIC_BASE_URL`

### What Still Requires Human Action (Mainnet Launch)

| Action | Status |
|--------|--------|
| Create mainnet admin wallet + fund with ≥ 5 ALGO | ⏳ Pending |
| Set all mainnet secrets in Replit Secrets panel | ⏳ Pending |
| First-boot: `FORCE_NEW_ASA=true` → record new ASA IDs | ⏳ Pending |
| Set `FORCE_NEW_ASA=false` after first boot | ⏳ Pending |
| Update `replit.md` Project Status with mainnet ASA IDs | ⏳ Pending |
| Confirm `PUBLIC_BASE_URL` = final deployed URL | ⏳ Pending |
| Full wallet + chain test on mainnet | ⏳ Pending |

### Carry to Next Session

- [ ] Create and fund mainnet admin wallet
- [ ] Set mainnet secrets in Replit
- [ ] First-boot mainnet test — record ASA IDs
- [ ] Consider Phase 1.1 (PlanetGlobe refactor) per ROADMAP.md
