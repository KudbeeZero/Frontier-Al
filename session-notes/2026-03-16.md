# Session Notes — 2026-03-16

## Branch
`claude/remove-checkly-organize-notes-ZYZg3`

---

## Tasks Completed

### 1. Checkly Removal
- Deleted entire `__checks__/` directory (contained Checkly monitors and browser checks)
  - `api.check.ts` — API check using `@checkly/constructs`
  - `url.check.ts` — URL monitor
  - `heartbeat.check.ts` — Heartbeat monitor
  - `homepage.spec.ts` — Checkly browser check (Playwright)
  - `custom.spec.ts` — Checkly browser check using `PUBLIC_BASE_URL`
- Removed `"checkly": "^5.4.0"` from `package.json` devDependencies
- No `checkly.config.ts` file existed at root

### 2. Session Notes Directory
- Created `session-notes/` directory at repo root
- Will be used to track all future sessions

---

## Current Status

### Public Base URL
- **No public base URL currently set.**
- The old URL `https://ascendancyalgo.xyz/` (found in `custom.spec.ts`) is no longer active / subject to change.
- `PUBLIC_BASE_URL` env var referenced in code — to be updated when a new URL is established.
- **Action needed:** Update `PUBLIC_BASE_URL` once new base URL is confirmed.

---

## Notes / Context

- This is a large-scale strategy game with:
  - 3D planetary map
  - Parcel system
  - AI factions
  - Blockchain interactions
- Tech stack: Node/TypeScript, React, Vite, PostgreSQL (Drizzle ORM), WebSockets, Passport auth, WalletConnect

---

## Next Steps (carry to next session)

- [ ] Confirm and set new `PUBLIC_BASE_URL` when available
- [ ] Review any remaining hardcoded URLs in the codebase
- [ ] Continue game development
